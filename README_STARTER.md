a-Lumen-nation (starter)
------------------------

Next steps:
1. Replace placeholders in src/services/firebaseClient.js with your Firebase web config.
2. Replace YOUR_FIREBASE_PROJECT_ID in .firebaserc.
3. Install dependencies and run via Termux (commands below).
4. Do not commit service account JSON files.

